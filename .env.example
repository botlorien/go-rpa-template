# ==========================================
# CONFIGURAÇÕES GERAIS DA APLICAÇÃO
# ==========================================

# Ambiente: local, dev, prod
# 'local' ativa logs coloridos; 'prod' ativa logs JSON.
APP_ENV=local

# Nível de Log: debug, info, warn, error
LOG_LEVEL=debug

# Porta onde a API (se usada) irá rodar
APP_PORT=8080

# URL Alvo principal (usada pelo Service)
TARGET_URL=https://target.com/login


# ==========================================
# MOTOR DE SCRAPING (ROD vs HTTP)
# ==========================================

# true = Usa navegador completo (Chrome/Rod) - Mais memória, mais lento, resolve JS.
# false = Usa apenas HTTP Client (Requests) - Leve e rápido.
USE_ROD=false

# true = Navegador invisível (Docker/Prod).
# false = Abre a janela do Chrome (Bom para debugar localmente).
ROD_HEADLESS=false


# ==========================================
# INTEGRAÇÃO BOTAPP (DASHBOARD)
# ==========================================
# Se deixar em branco, o robô roda em modo "offline" (sem enviar logs para a dashboard)

BOTAPP_API_URL=http://localhost:8000/api
BOTAPP_API_USUARIO=admin
BOTAPP_API_SENHA=admin


# ==========================================
# INPUTS DO ROBÔ (Variáveis de Negócio)
# ==========================================
# Estas variáveis são mapeadas no main.go para a struct ExecutionInput.
# Usadas quando rodando via CLI/Docker/GitLab CI.

# Credenciais do sistema alvo (ex: SSW, Portal Fiscal)
RPA_USERNAME=usuario_teste
RPA_PASSWORD=senha_teste
RPA_TOKEN=

# Parâmetros de execução
RPA_DATA_INICIO=2023-10-01
RPA_FILTRO_ID=999
RPA_BAIXAR_PDF=true